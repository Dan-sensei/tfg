import { Montserrat, Tulpen_One, Bebas_Neue } from 'next/font/google'

export const montserrat = Montserrat({subsets: ['latin']});
export const tulpen_one = Tulpen_One({subsets: ['latin'], weight: '400'});
export const bebas_Neue = Bebas_Neue({subsets: ['latin'], weight: '400'});